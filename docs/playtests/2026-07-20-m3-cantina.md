# M3 Cantina production playtest — 2026-07-20

Verified against implementation HEAD
`7ed3e1856b663729cb98f46bc05ad32c914f29ff` on
`feat/m3-cantina-runtime`.

## Build and serve

The browser pass used the DevTools static export verified immediately beforehand by the
Task 7 verifier at the same HEAD. Those gates were not rerun during this browser-only
task.

| Command (from `game/`) | Task 7 result |
|---|---:|
| `npx tsc --noEmit` | pass |
| `yarn colony:test` | 301/301 passed |
| `yarn engine:test` | 309/309 passed |
| `yarn sprites:test` | 8/8 passed |
| `yarn build` | pass; static pages 6/6, export 3/3 |
| `NEXT_PUBLIC_DEVTOOLS=1 yarn build` | pass; static pages 6/6, export 3/3 |

The verified DevTools export was served with:

```bash
cd /private/tmp/sector-zero-m3-cantina-runtime/game
npx serve out -l 3000
```

Port 3000 resolved to PID 25535, whose process command was `serve out -l 3000` and whose
working directory was `/private/tmp/sector-zero-m3-cantina-runtime/game`. A request to
`http://localhost:3000/` returned the Sector Zero static export and its production chunk
paths.

## Browser and screenshot

- Browser: installed Google Chrome 150.0.7871.125, driven through Playwright in the
  persistent Node REPL.
- Profile: fresh temporary Chrome profile created for this playtest; the persistent
  profile prevented an automation-session restart from substituting injected save state.
- CSS viewport: exactly 480×854.
- Device pixel ratio: 1.
- Screenshot pixels: exactly 480×854, 8-bit RGB PNG, non-interlaced.
- Screenshot: `docs/assets/reviews/m3-hubs/cantina/gameplay-480x854.png`.
- SHA-256: `a4715ad4a0dc01227e1c94de89ea35c0bf91041e64b4a46cde5aa9a81164675b`.

The screenshot shows the authored wall, floor, and ceiling, the bar counter and bottle
rack, and the bartender in one unobscured gameplay frame.

## Normal commission and access path

1. Began a fresh Legacy Campaign and reached the cockpit.
2. Used the `SEED REGION` fixture only to bypass the founding/resource grind. It supplied
   one otherwise empty colony at cycle 0 with 600 metal; it did not prebuild the Cantina.
3. Took off from the real landing pad, followed Cockpit → Colonies → Colony Detail, and
   commissioned Cantina through its real menu entry. Metal changed 600 → 400 and the
   building showed `constructing` with three cycles remaining.
4. Completed three real mission-result flows, using DevPanel wave shortcuts only to
   accelerate combat. The mission cycle and construction observations were:

| Result flow | Colony cycle | Cantina state |
|---|---:|---|
| before missions | 0 | constructing, 3 remaining |
| W1-L1 result | 1 | constructing, 2 remaining |
| W1-L2 result | 2 | constructing, 1 remaining |
| W1-L1 replay result | 3 | operational, 0 remaining |

5. Returned through Cockpit → Colonies → Colony Detail → Descend, walked from the real
   landing pad to the east-mid Cantina facade, and entered its south door. No direct
   interior teleport or Atlas route was used.
6. The authored Cantina interior rendered, and using the south exit restored the exterior
   one tile south of the door, facing away from the facade.

## Deterministic `SEED CANTINA` path

The fixture produced hour 17:00, exactly 5 player credits, and operational solar array,
farm, water purifier, habitat module, and Cantina buildings in that order. The playtest
again descended to the exterior, walked to the real east-mid facade, and entered the real
south door.

The exterior Cantina facade and the distinct interior wall, floor, and ceiling all decoded
and rendered. The interior deliberately showed no sky. Browser resource timing contained
all 14 registered Cantina resources — facade, three environment textures, four props,
three NPC billboards, and three portraits — each with a positive decoded body size.

## Prop presentation

All props remained on the existing square billboard plane (`widthFactor: 1`). At the
actual 480×854 viewport, their transparent padding preserved the subject proportions; no
prop blocked the aisle, clipped badly, or read as stretched.

| Prop | Authored tile | Scale | Browser observation |
|---|---:|---:|---|
| Bottle rack | (2, 1) | 1.0 | Upright bottles and rack remained proportionate behind the bartender. |
| Bar counter | (4, 3) | 1.4 | Wide counter read cleanly across the service alcove without vertical stretch or aisle blockage. |
| Table cluster | (3, 5) | 1.2 | Circular table cluster remained round and legible beside the regular. |
| Rumor terminal | (8, 5) | 1.0 | Cabinet and cyan display remained proportionate beside the signal-chaser. |

Facing the rumor terminal and pressing interact produced no panel or transition. It is a
visual-only prop in this slice.

## Period-0 roles, billboards, and portraits

All three 06:00–18:00 roles were present at 17:00. The anchors below are authored tile
anchors; the generated billboards render at the corresponding tile centers.

| Role | Anchor | Billboard | Portrait | Browser observation |
|---|---:|---|---|---|
| Bartender | (3, 2) | `/sprites/boarding/npc-hub-bartender.png` | `/sprites/portraits/hub-bartender.png` | Billboard, orange merchant marker, portrait dialog, and House Pour shop all rendered. |
| Regular | (4, 6) | `/sprites/boarding/npc-hub-regular.png` | `/sprites/portraits/hub-regular.png` | Billboard, cyan lore marker, portrait, and rumor rendered beside the table cluster. |
| Signal-chaser | (9, 5) | `/sprites/boarding/npc-hub-signal-chaser.png` | `/sprites/portraits/hub-signal-chaser.png` | Billboard, purple lore marker, portrait, and rumor rendered beside the terminal. |

At the exact 480×854 viewport, all three dialog portraits were identifiable and legible:
the bartender's face and apron, the regular's face and coat, and the signal-chaser's face
and dark suit remained distinct beside their speaker names and dialog text.

The regular said `The old relay wakes just before the dust turns.` on two separate entries
using the same fixture inputs. The signal-chaser said
`I caught our colony beacon echoing from below the ridge.` No rumor changed the save.

## House Pour ownership and feedback

- The bartender shop displayed House Pour for 5 credits.
- First purchase: credits 5 → 0 and green `HOUSE POUR SERVED` appeared.
- Second purchase: credits 0 → 0 and red `PURCHASE UNAVAILABLE` appeared.
- The rejected-purchase save snapshot was byte-identical before and after.
- Comparing the successful-purchase snapshots found exactly one changed top-level save
  key: `credits`. `consumableInventory` remained `{}`, and both `activeQuests` and
  `completedQuests` remained `[]`; no item, buff, or quest was created.
- The red failure replaced the prior green message, then expired after its 90-frame
  window without leaving stale text.

The seeded interior was exited normally through the south door and restored the colony
exterior.

## Console status

- Console errors: 0.
- Page errors: 0.
- Console warnings: 0.
- Informational logs: one expected `PILOT LEVEL UP! 1 → 2` emitted during the
  normal-path mission-cycle proof.

## Residual gaps

None for the required Task 8 acceptance path. Marketplace, Town Hall, bulletin-board
quests, Atlas-to-colony travel, and other explicit design non-goals were not claimed or
exercised. Period-1 placement remains covered by the automated schedule tests; Task 8's
browser checklist intentionally exercised the 17:00 period-0 fixture.
